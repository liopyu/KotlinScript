package net.liopyu.kotlinscript;

import net.minecraftforge.event.entity.player.PlayerInteractEvent;

import java.util.ArrayList;
import java.util.List;
import java.util.function.Consumer;

public class CustomScriptEventManager {
    /*private static List<Consumer<PlayerInteractEvent.RightClickItem>> rightClickConsumers = new ArrayList<>();

    public static void onRightClicked(Consumer<PlayerInteractEvent.RightClickItem> consumer) {
        rightClickConsumers.add(consumer);
    }

    public static void triggerRightClick(PlayerInteractEvent.RightClickItem event) {
        rightClickConsumers.forEach(consumer -> consumer.accept(event));
    }*/
}